services:
  check-cx:
    image: llpig/check-cx:latest
    container_name: check-cx
    restart: unless-stopped
    ports:
      - "3007:3000"
    env_file:
      - .env
    environment:
      - NODE_ENV=production
